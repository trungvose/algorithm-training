import { slowestKey } from './1629. Slowest Key';

describe('5546. Slowest Key', () => {
  it('should pass the first test', () => {
    expect(slowestKey([12, 23, 36, 46, 62], 'spuda')).toBe('a');
  });
});
