import { minOperations } from './1598. Crawler Log Folder'

describe('5523. Crawler Log Folder.ts', () => {
  it('Test', () => {
    expect(
      minOperations(['./', 'wz4/', '../', 'mj2/', '../', '../', 'ik0/', 'il7/'])
    ).toBe(2)
  })
})
