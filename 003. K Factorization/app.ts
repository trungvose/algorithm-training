import { ReadFile } from "../Common/common";

ReadFile.Read(data => KFactorization.Main(data))

class KFactorization {
    private static n: number;
    private static k: number;
    private static a: number[];
    
    public static Main(data) {

    }
}